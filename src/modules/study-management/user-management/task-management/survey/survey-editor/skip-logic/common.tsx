import styled from 'styled-components';
import Dropdown from 'src/common/components/Dropdown';

export const SkipLogicDropdown = styled(Dropdown).attrs({
  backgroundType: 'light',
  menuItemHeight: 48,
})`` as typeof Dropdown;
